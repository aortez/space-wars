// Root window for the engine-client.
//
// Empty for M4: just a dark canvas at the default resolution. Custom drawing,
// HUD, menus, and per-scenario overlays land in later milestones.

export component MainWindow inherits Window {
    title: "Space-Wars";
    preferred-width: 1280px;
    preferred-height: 720px;
    background: #050514;
}
